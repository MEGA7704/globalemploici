{
  "name": "global-emploi",
  "version": "2.8.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev dist --compatibility-date=2026-08-07",
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist",
    "test:smoke": "python3 scripts/verify_route_contracts.py && python3 scripts/smoke_pages.py"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
